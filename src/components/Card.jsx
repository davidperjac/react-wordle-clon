import { Center, Title } from '@mantine/core';

const Card = () => {
	return (
		<Center style={{ width: 55, height: 55, border: '2px solid grey' }}>
			<Title>A</Title>
		</Center>
	);
};

export default Card;
